DATABASE_URL=
# Auth.js (NextAuth v5) — generate with: openssl rand -base64 32
AUTH_SECRET=
# Admin seed (db:seed) — set real values locally, never commit them
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_NAME=
